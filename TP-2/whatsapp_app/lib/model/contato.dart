class Contato {
  int id;
  String nome;
  var icon;
  String descricao;
  DateTime dataUltimaMensagem;
  String numeroTelefone;
  Contato(this.id, this.nome, this.icon, this.descricao,
      this.dataUltimaMensagem, this.numeroTelefone);
}
